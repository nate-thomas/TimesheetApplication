@model IEnumerable<TimeSheetApplication.Models.TimeSheetSystem.LaborGrades>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.PayAmount)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Grade)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PayAmount)
        </td>
        <td>
            <a asp-action="Edit" asp-route-id="@item.Grade">Edit</a> |
            <a asp-action="Details" asp-route-id="@item.Grade">Details</a> |
            <a asp-action="Delete" asp-route-id="@item.Grade">Delete</a>
        </td>
    </tr>
}
    </tbody>
</table>
